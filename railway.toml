[build]
builder = "nixpacks"

[deploy]
startCommand = "uv run alembic upgrade head && uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
